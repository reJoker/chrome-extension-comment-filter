{
  "manifest_version": 2,

  "name": "chrome-extension: Comment filter",
  "description": "A chrome extension which could filter out comments of selected users",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab",
    "https://*.google.com/"
  ]
}
